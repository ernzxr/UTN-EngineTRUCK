import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    manufacturer: ""
}

export const manufacturerSlice = createSlice({
    name: "manufacturer",
    initialState,
    reducers: {
        
    }
})